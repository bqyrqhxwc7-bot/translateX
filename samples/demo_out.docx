翻译工作台使用手册
—— 面向翻译写作场景的完整指南 ——

第一章 翻译工作流程
本章介绍 translateX 的核心工作流程：从打开文档、逐行翻译，到批注与保存。
工具支持三种翻译后端：本地 Ollama、云端翻译服务与网络大模型 API，可根据成本与质量需求灵活切换；重复内容自动命中缓存，显著降低成本。
1. 打开文档：支持 .txt、.trx 与 .docx 格式，其中 .trx 为推荐格式，富文本与图片显示层可完整往返保存。
2. 选择后端：本地 Ollama 零成本、云端服务免费额度、网络大模型质量最高。
提示：批量翻译自动分块合并请求，失败单行自动重试并降级。
3. 逐行翻译：上下文感知翻译保持术语与语气一致，质量自检拦截回显输出。
流程要点 1：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
流程要点 2：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
流程要点 3：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
流程要点 4：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
流程要点 5：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
流程要点 6：确保上下文窗口包含前后若干行，译文语气与术语保持一致；翻译结果作为批注写入对应行，可随时上一条/下一条跳转复核。
第二章 术语一致性
术语表是保证翻译质量一致性的关键。用户可维护“原文术语 → 标准译文”映射，翻译前注入约束提示，翻译后自动校验术语命中率。
示例术语：API → 接口 ；context → 上下文 ；batch → 批量 。
未命中术语会列入报告：例如译文漏译术语时将提示 missing: API，便于人工复核。
字号演示：特大 大 中 小 极小 
术语条目 1：在术语表中维护后，后续所有翻译请求自动携带该约束，保证整篇文档内同一概念始终使用同一译法。
术语条目 2：在术语表中维护后，后续所有翻译请求自动携带该约束，保证整篇文档内同一概念始终使用同一译法。
术语条目 3：在术语表中维护后，后续所有翻译请求自动携带该约束，保证整篇文档内同一概念始终使用同一译法。
术语条目 4：在术语表中维护后，后续所有翻译请求自动携带该约束，保证整篇文档内同一概念始终使用同一译法。
术语条目 5：在术语表中维护后，后续所有翻译请求自动携带该约束，保证整篇文档内同一概念始终使用同一译法。
第三章 质量与成本
质量自检覆盖回显拦截、长度合理性、数字与占位符保留；成本控制依赖缓存复用、智能分块与模型分级。
质量规则：译文不得直接回显原文；
图片显示层支持行内插图，.trx 保存时以内嵌 base64 往返：


示例插图：上图演示了行内图片与文字混排的效果。
优化建议 1：对重复段落启用缓存，对长文本启用智能分块合并请求，对低风险文本优先使用免费后端，整体翻译成本可降低 40% 以上。
优化建议 2：对重复段落启用缓存，对长文本启用智能分块合并请求，对低风险文本优先使用免费后端，整体翻译成本可降低 40% 以上。
优化建议 3：对重复段落启用缓存，对长文本启用智能分块合并请求，对低风险文本优先使用免费后端，整体翻译成本可降低 40% 以上。
优化建议 4：对重复段落启用缓存，对长文本启用智能分块合并请求，对低风险文本优先使用免费后端，整体翻译成本可降低 40% 以上。
优化建议 5：对重复段落启用缓存，对长文本启用智能分块合并请求，对低风险文本优先使用免费后端，整体翻译成本可降低 40% 以上。
第四章 批注与协作
批注（译文）与原文一一对应，行内直接编辑，字号独立可调；支持批注的增删、跳转、清空与 JSON 导入导出。
行内编辑：点击批注即可像编辑原文一样直接修改，超过宽度自动换行；
快捷键：Enter 拆行、行首 Backspace 合并、右键菜单提供插入/删除/复制等操作。

—— 全文完 ——
